# todo-api-springboot
This is a backend service for a Task Management system built using Spring Boot. It provides secure JWT-based authentication and REST APIs to create, update, delete, and manage tasks. The application uses PostgreSQL for data persistence and follows a clean layered architecture with proper exception handling.
